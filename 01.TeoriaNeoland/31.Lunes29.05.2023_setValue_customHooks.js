//? setValue

// Limpiar App.jsx, estilos, App.css, index.css y hacer * Margin 0, padding 0 y box.sizen borderbox y fuera asset e icono (favicon)

//? Custom Hooks ReactJS

//! Los Hooks empiezan siempre con use... ejemplo useState o useEffect

// Es una función que exporta cosas

// useEffect es
// No actualizar estados dentro del useEffect porque hace un bucle infinito

// SI NO HAY LLAVES NO HACE FALTA PONER RETURN ejemplo: punto 6.- onCLick en CounterWithCustomHook

// Ver en react-bootstrap ya que viene muchas cosas maquetadas

//Simple FOrm

// En bootstrap mt-2 es para darle un apequeña separación en classname de email
// value es el valor de cada input
// evento onChange es cuando cambia el valor de algo y hay que meter una callback
//

// Un componenete no acoplado es que es reutilizable
